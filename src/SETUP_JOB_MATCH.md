# 🚀 دليل إعداد ميزة نسبة التوافق مع الوظائف

## الخطوات المطلوبة

### 1️⃣ تحديث قاعدة البيانات Supabase

انتقل إلى **Supabase Dashboard** → **SQL Editor** → **New Query**

الصق الكود التالي ونفذه:

```sql
-- ====================================
-- 📝 تحديث جدول user_profiles لدعم ميزة نسبة التوافق
-- ====================================

-- إضافة حقول جديدة إذا لم تكن موجودة
ALTER TABLE user_profiles 
ADD COLUMN IF NOT EXISTS skills TEXT,
ADD COLUMN IF NOT EXISTS experience TEXT,
ADD COLUMN IF NOT EXISTS specialty TEXT,
ADD COLUMN IF NOT EXISTS location TEXT,
ADD COLUMN IF NOT EXISTS education TEXT,
ADD COLUMN IF NOT EXISTS bio TEXT,
ADD COLUMN IF NOT EXISTS phone TEXT;

-- إضافة indexes للبحث السريع (اختياري ولكن موصى به)
CREATE INDEX IF NOT EXISTS idx_user_profiles_user_id ON user_profiles(user_id);
CREATE INDEX IF NOT EXISTS idx_user_profiles_specialty ON user_profiles(specialty);
CREATE INDEX IF NOT EXISTS idx_user_profiles_location ON user_profiles(location);
```

✅ بعد التنفيذ، يجب أن تظهر رسالة "Success"

---

### 2️⃣ التحقق من RLS Policies (اختياري)

تأكد من أن المستخدمين يمكنهم قراءة وتحديث بياناتهم:

```sql
-- السماح للمستخدم بقراءة بياناته
CREATE POLICY IF NOT EXISTS "Users can read own profile"
ON user_profiles FOR SELECT
USING (auth.uid()::text = user_id);

-- السماح للمستخدم بتحديث بياناته
CREATE POLICY IF NOT EXISTS "Users can update own profile"
ON user_profiles FOR UPDATE
USING (auth.uid()::text = user_id);

-- السماح للـ service_role بكل شيء
CREATE POLICY IF NOT EXISTS "Service role full access"
ON user_profiles FOR ALL
TO service_role
USING (true)
WITH CHECK (true);
```

---

### 3️⃣ اختبار الميزة

#### أ) تسجيل دخول كمستخدم Premium:

1. سجل دخول إلى حساب له اشتراك Premium نشط
2. انتقل إلى صفحة **الملف الشخصي** (`/profile`)
3. املأ البيانات التالية:
   - **المهارات**: مثال: `JavaScript, React, تطوير الويب, تصميم`
   - **سنوات الخبرة**: مثال: `5 سنوات خبرة في تطوير التطبيقات`
   - **التخصص**: مثال: `تطوير البرمجيات`
   - **الموقع**: مثال: `مسقط`
   - **المؤهل الدراسي**: مثال: `بكالوريوس علوم الحاسوب`

4. احفظ البيانات

#### ب) اختبار النسب المئوية:

1. انتقل إلى الصفحة الرئيسية (`/`)
2. يجب أن تظهر **نسبة مئوية ملونة** في الزاوية العلوية اليسرى لكل وظيفة
3. الألوان:
   - 🟢 **أخضر (80%+)** = توافق ممتاز
   - 🟡 **أصفر (60-79%)** = توافق جيد
   - 🟠 **برتقالي (أقل من 60%)** = توافق متوسط

---

### 4️⃣ ما المتوقع رؤيته؟

#### للمستخدمين Premium مع بروفايل كامل:
![نسبة التوافق](./figma:asset/c51024d6d8a150431218be1d4805301b2f272e93.png)

- نسبة مئوية في أعلى اليسار من البطاقة
- مع أيقونة سهم صاعد (TrendingUp)
- ألوان تعبيرية حسب النسبة

#### للمستخدمين العاديين (غير Premium):
- بطاقات الوظائف العادية فقط
- بدون نسب التوافق

---

## 🧪 اختبارات إضافية

### Test Case 1: مهارات عالية التوافق
```
بيانات المستخدم:
- المهارات: "JavaScript, React, Node.js, MongoDB"
- الخبرة: "5 سنوات"
- التخصص: "تطوير البرمجيات"

الوظيفة المناسبة:
- العنوان: "مطور Full Stack"
- المتطلبات: "خبرة في React و Node.js"

النتيجة المتوقعة: 85-95%
```

### Test Case 2: توافق متوسط
```
بيانات المستخدم:
- المهارات: "التسويق الرقمي, SEO"
- الخبرة: "سنة واحدة"
- التخصص: "تسويق"

الوظيفة:
- العنوان: "مدير تسويق أول"
- المتطلبات: "5+ سنوات خبرة"

النتيجة المتوقعة: 45-65%
```

---

## 🔍 استكشاف الأخطاء

### المشكلة: لا تظهر النسب المئوية

**الحلول المحتملة:**

1. **التحقق من Premium:**
   ```javascript
   // في Console المتصفح
   // افتح DevTools (F12) واكتب:
   console.log('isPremium:', isPremium);
   ```

2. **التحقق من البروفايل:**
   ```javascript
   // في Console المتصفح
   console.log('userProfile:', userProfile);
   // يجب أن يعرض: { skills: "...", experience: "...", ... }
   ```

3. **التحقق من حساب النسب:**
   ```javascript
   // في Console المتصفح
   console.log('jobMatches:', jobMatches);
   // يجب أن يعرض: { "job-id-1": 85, "job-id-2": 72, ... }
   ```

4. **التحقق من SQL:**
   - افتح Supabase → Table Editor → `user_profiles`
   - تأكد من وجود الحقول الجديدة (skills, experience, specialty...)

---

### المشكلة: النسب المئوية غير دقيقة

**الحلول:**

1. **تحديث بيانات البروفايل:**
   - تأكد من ملء جميع الحقول بشكل صحيح
   - استخدم كلمات مفتاحية واضحة في المهارات

2. **تحسين محتوى الوظائف:**
   - تأكد من أن الوظائف لها `requirements` و `description` مفصلة

3. **ضبط الخوارزمية (اختياري):**
   - افتح `/utils/jobMatchCalculator.ts`
   - عدّل الأوزان في الدالة `calculateJobMatch()`

---

## 📊 فهم الخوارزمية

### كيف تُحسب النسبة؟

النسبة الإجمالية = مجموع (النسبة × الوزن) لكل بُعد:

```
النسبة النهائية = 
  (نسبة المهارات × 40%) +
  (نسبة الخبرة × 25%) +
  (نسبة التخصص × 20%) +
  (نسبة الموقع × 10%) +
  (نسبة المؤهلات × 5%)
```

**مثال:**
```
المهارات: 90% × 0.4 = 36
الخبرة: 100% × 0.25 = 25
التخصص: 85% × 0.2 = 17
الموقع: 90% × 0.1 = 9
المؤهلات: 100% × 0.05 = 5
─────────────────────────
الإجمالي = 92%
```

---

## 💡 نصائح للحصول على نسب أعلى

### للمستخدمين:

1. **املأ بروفايلك بالكامل**
   - كلما زادت المعلومات، كلما كانت النسبة أدق

2. **استخدم كلمات مفتاحية واضحة**
   - بدلاً من "أعمل في الحاسوب"
   - اكتب: "مطور ويب متخصص في React و Node.js"

3. **حدّث بياناتك باستمرار**
   - أضف مهارات جديدة
   - حدّث سنوات الخبرة

4. **كن محددًا**
   - "5 سنوات خبرة في تطوير تطبيقات الموبايل"
   - أفضل من "خبرة في البرمجة"

---

## 🎯 الأهداف المحققة

### ✅ للمستخدمين:
- معرفة فورية بمدى التوافق مع كل وظيفة
- توفير الوقت في البحث
- زيادة فرص القبول بالتركيز على الوظائف المناسبة

### ✅ للمنصة:
- قيمة مضافة حقيقية للمستخدمين Premium
- تشجيع على الاشتراك في Premium
- تحسين تجربة المستخدم بشكل كبير
- تميز عن منصات التوظيف الأخرى

---

## 📞 الدعم

إذا واجهت أي مشكلة:

1. تأكد من تنفيذ SQL في الخطوة 1
2. تأكد من ملء بيانات البروفايل
3. تأكد من وجود اشتراك Premium نشط
4. افتح Console المتصفح (F12) لرؤية أي أخطاء
5. راجع ملف `/JOB_MATCH_ALGORITHM_COMPLETE.md` للتفاصيل الكاملة

---

**استمتع بميزة نسبة التوافق الذكية! 🚀**
