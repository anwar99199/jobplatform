# 📋 ملخص شامل لمشروع منصة عُمان للوظائف

<div dir="rtl">

## 📌 نظرة عامة

**منصة عُمان للوظائف** هي موقع إلكتروني متكامل لنشر الوظائف مع خدمات Premium مدعومة بالذكاء الاصطناعي، مصمم خصيصاً للسوق العُماني مع دعم كامل للغة العربية وتوجيه RTL.

---

## 🛠️ التقنيات المستخدمة

### **Frontend (الواجهة الأمامية)**

| التقنية | الإصدار | الاستخدام |
|---------|---------|-----------|
| **React** | 18.3.1 | مكتبة JavaScript لبناء واجهات المستخدم |
| **TypeScript** | 5.6.2 | لغة برمجة مكتوبة statically typed |
| **React Router** | 7.9.4 | للتنقل بين الصفحات (SPA routing) |
| **Tailwind CSS** | 4.0 | إطار عمل CSS للتصميم السريع |
| **Vite** | 6.2.1 | أداة بناء سريعة (build tool) |

### **UI Components & Libraries**

| المكتبة | الاستخدام |
|---------|-----------|
| **Shadcn UI** | مكتبة مكونات UI جاهزة (Button, Input, Dialog, etc.) |
| **Lucide React** | أيقونات SVG (Icons) |
| **Recharts** | رسوم بيانية (Charts) للإحصائيات |
| **Sonner** | إشعارات Toast notifications |
| **date-fns** | معالجة التواريخ والأوقات |

### **Backend (الخادم الخلفي)**

| التقنية | الوصف |
|---------|--------|
| **Supabase** | منصة BaaS (Backend as a Service) |
| **PostgreSQL** | قاعدة بيانات SQL |
| **Supabase Edge Functions** | Serverless functions |
| **Hono Framework** | إطار عمل Web خفيف للـ Edge Functions |
| **Deno Runtime** | بيئة تشغيل JavaScript/TypeScript للسيرفر |

### **AI & External Services**

| الخدمة | الاستخدام |
|--------|-----------|
| **OpenAI GPT-4o-mini** | توليد Cover Letters، تحليل السير الذاتية، حساب التوافق |
| **PDF-Parse** | استخراج النص من ملفات PDF |
| **Mammoth.js** | استخراج النص من ملفات DOCX |

### **Storage & Authentication**

| الخدمة | الوصف |
|--------|--------|
| **Supabase Storage** | تخزين الملفات (صور، سير ذاتية، مستندات) |
| **Supabase Auth** | نظام المصادقة للمستخدمين |
| **JWT Tokens** | للتحقق من الهوية |
| **Row Level Security (RLS)** | حماية البيانات على مستوى الصفوف |

### **Deployment (الاستضافة)**

| المنصة | الاستخدام |
|--------|-----------|
| **Vercel** | استضافة Frontend |
| **Supabase Cloud** | استضافة Backend + Database + Storage |

---

## 📁 هيكل المشروع

```
oman-jobs-platform/
│
├── 📂 pages/                          # جميع صفحات التطبيق
│   ├── HomePage.tsx                   # الصفحة الرئيسية
│   ├── AboutPage.tsx                  # صفحة من نحن
│   ├── ContactPage.tsx                # صفحة التواصل
│   ├── PremiumPage.tsx                # صفحة الخدمات المميزة
│   ├── LoginPage.tsx                  # تسجيل الدخول
│   ├── RegisterPage.tsx               # التسجيل
│   ├── ProfilePage.tsx                # الملف الشخصي
│   ├── JobDetailsPage.tsx             # تفاصيل الوظيفة
│   ├── CompanyJobsPage.tsx            # وظائف الشركات
│   ├── GovernmentJobsPage.tsx         # الوظائف الحكومية
│   ├── NewsPage.tsx                   # الأخبار
│   ├── CoverLetterPage.tsx            # أداة Cover Letter (Premium)
│   ├── DigitalCardPage.tsx            # البطاقة الرقمية (Premium)
│   ├── JobMatchPage.tsx               # نسبة التوافق (Premium)
│   ├── ATSConverterPage.tsx           # محول ATS (Premium)
│   ├── PrivacyPolicyPage.tsx          # سياسة الخصوصية
│   ├── TermsOfServicePage.tsx         # شروط الخدمة
│   │
│   └── 📂 admin/                      # لوحة تحكم الإدارة
│       ├── AdminLoginPage.tsx         # تسجيل دخول Admin
│       ├── AdminDashboardPage.tsx     # لوحة المعلومات
│       ├── AdminJobsPage.tsx          # إدارة الوظائف
│       ├── AdminJobFormPage.tsx       # إضافة/تعديل وظيفة
│       ├── AdminUsersPage.tsx         # إدارة المستخدمين
│       ├── AdminNewsPage.tsx          # إدارة الأخبار
│       ├── AdminContactPage.tsx       # رسائل التواصل
│       └── AdminAnalyticsPage.tsx     # الإحصائيات والتحليلات
│
├── 📂 components/                     # المكونات القابلة لإعادة الاستخدام
│   ├── Header.tsx                     # شريط التنقل العلوي
│   ├── Footer.tsx                     # التذييل
│   ├── Hero.tsx                       # قسم البطل (Hero Section)
│   ├── JobsSection.tsx                # عرض الوظائف
│   ├── Layout.tsx                     # تخطيط الصفحة العام
│   ├── ProtectedRoute.tsx             # حماية الصفحات (Auth Guard)
│   ├── CoverLetterGenerator.tsx       # مولد Cover Letter
│   ├── UploadCV.tsx                   # رفع السيرة الذاتية
│   ├── ConvertedCV.tsx                # عرض CV المحول
│   ├── PremiumFloatingPromo.tsx       # إعلان Premium عائم
│   │
│   ├── 📂 ui/                         # مكونات Shadcn UI
│   │   ├── button.tsx                 # زر
│   │   ├── input.tsx                  # حقل إدخال
│   │   ├── card.tsx                   # بطاقة
│   │   ├── dialog.tsx                 # نافذة حوارية
│   │   ├── table.tsx                  # جدول
│   │   ├── badge.tsx                  # وسم
│   │   ├── select.tsx                 # قائمة منسدلة
│   │   ├── textarea.tsx               # مربع نص
│   │   ├── label.tsx                  # تسمية
│   │   ├── tabs.tsx                   # تبويبات
│   │   ├── avatar.tsx                 # صورة شخصية
│   │   ├── switch.tsx                 # مفتاح تبديل
│   │   ├── progress.tsx               # شريط تقدم
│   │   ├── skeleton.tsx               # عنصر تحميل
│   │   └── ...                        # 30+ مكون آخر
│   │
│   └── 📂 figma/
│       └── ImageWithFallback.tsx      # صورة مع احتياطي
│
├── 📂 utils/                          # وظائف مساعدة و API
│   ├── api.ts                         # استدعاءات API الرئيسية
│   ├── adminApi.ts                    # API خاص بالـ Admin
│   ├── cover-letter-api.ts            # API توليد Cover Letter
│   ├── ats-converter-api.ts           # API محول ATS
│   ├── jobMatcher.ts                  # حساب نسبة التوافق
│   ├── jobMatchCalculator.ts          # خوارزمية التوافق
│   ├── extractText.ts                 # استخراج النص من الملفات
│   ├── downloadFile.ts                # تحميل الملفات
│   │
│   └── 📂 supabase/
│       ├── client.ts                  # Supabase client للـ Frontend
│       ├── client.tsx                 # Supabase client (نسخة بديلة)
│       └── info.tsx                   # معلومات المشروع (projectId, keys)
│
├── 📂 supabase/                       # ملفات Supabase
│   ├── 📂 functions/server/           # Edge Functions (Backend)
│   │   ├── index.tsx                  # الملف الرئيسي للسيرفر (Hono app)
│   ���   ├── kv_store.tsx               # وظائف KV Store (get, set, del)
│   │   ├── pdf-extractor.tsx          # استخراج نص PDF/DOCX
│   │   └── pdf-simple-extractor.tsx   # مستخرج PDF بسيط
│   │
│   └── 📂 migrations/                 # SQL Scripts
│       ├── create_contact_messages.sql
│       └── add_requirements_to_jobs.sql
│
├── 📂 styles/
│   └── globals.css                    # أنماط CSS العامة + Tailwind
│
├── 📄 App.tsx                         # المكون الرئيسي للتطبيق
├── 📄 vercel.json                     # إعدادات النشر على Vercel
├── 📄 package.json                    # الحزم والتبعيات
├── 📄 tsconfig.json                   # إعدادات TypeScript
├── 📄 tailwind.config.js              # (غير موجود - نستخدم Tailwind v4)
├── 📄 vite.config.ts                  # إعدادات Vite
├── 📄 .gitignore                      # ملفات متجاهلة
├── 📄 .env.local                      # المتغيرات البيئية (محلي - غير مشارك)
│
└── 📂 Documentation/                  # ملفات التوثيق
    ├── README.md                      # نظرة عامة على المشروع
    ├── SETUP.md                       # دليل الإعداد المفصل
    ├── START_HERE.md                  # دليل البداية
    ├── READY_TO_USE.md                # دليل الاستخدام السريع
    ├── SHARING_GUIDE.md               # دليل المشاركة الآمنة
    ├── PROJECT_CLEAN_STATUS.md        # حالة المشروع
    ├── AUTHENTICATION_INFO.md         # معلومات المصادقة
    └── ...                            # 30+ ملف توثيق آخر
```

---

## 🗄️ قاعدة البيانات (Supabase PostgreSQL)

### **الجداول الرئيسية:**

#### 1. **جدول `users` - المستخدمين**
```sql
- id (TEXT) - معرف فريد
- email (TEXT) - البريد الإلكتروني (فريد)
- name (TEXT) - الاسم
- role (TEXT) - الدور (user)
- created_at (TIMESTAMPTZ)
- updated_at (TIMESTAMPTZ)
```

#### 2. **جدول `user_profiles` - ملفات المستخدمين**
```sql
- id (TEXT) - معرف فريد
- user_id (TEXT) - مرجع للمستخدم
- phone (TEXT)
- skills (TEXT[]) - المهارات
- experience (TEXT)
- education (TEXT)
- linkedin (TEXT)
- github (TEXT)
- portfolio (TEXT)
- created_at/updated_at
```

#### 3. **جدول `jobs` - الوظائف**
```sql
- id (UUID) - معرف فريد تلقائي
- title (TEXT) - المسمى الوظيفي ✅
- company (TEXT) - اسم المؤسسة (قيمة افتراضية: "غير محدد")
- location (TEXT) - الموقع (قيمة افتراضية: "عُمان")
- type (TEXT) - نوع الوظيفة (قيمة افتراضية: "غير محدد")
- description (TEXT) - وصف الوظيفة ✅
- requirements (TEXT) - المتطلبات
- application_url (TEXT) - رابط التقديم ✅
- date (DATE) - تاريخ النشر ✅
- created_at (TIMESTAMPTZ)
```
**ملاحظة:** الحقول المميزة بـ ✅ هي الحقول الفعلية المستخدمة حالياً

#### 4. **جدول `premium_subscriptions` - الاشتراكات المميزة**
```sql
- id (TEXT)
- user_id (TEXT)
- plan_type (TEXT) - monthly أو yearly
- start_date (DATE)
- end_date (DATE)
- status (TEXT) - active, expired, cancelled
- price (NUMERIC)
- created_at/updated_at
```

#### 5. **جدول `digital_cards` - البطاقات الرقمية**
```sql
- id (TEXT)
- user_id (TEXT)
- name (TEXT)
- title (TEXT) - المسمى الوظيفي
- bio (TEXT) - نبذة
- email/phone/linkedin/github/portfolio
- profile_image_url (TEXT)
- cv_url (TEXT)
- skills (TEXT[])
- created_at/updated_at
```

#### 6. **جدول `news` - الأخبار**
```sql
- id (TEXT)
- title (TEXT)
- content (TEXT)
- image_url (TEXT)
- category (TEXT)
- author (TEXT)
- published_date (DATE)
- is_featured (BOOLEAN)
- created_at/updated_at
```

#### 7. **جدول `contact_messages` - رسائل التواصل**
```sql
- id (UUID)
- name (TEXT)
- email (TEXT)
- phone (TEXT)
- subject (TEXT)
- message (TEXT)
- status (TEXT) - new, read, replied
- admin_reply (TEXT)
- created_at (TIMESTAMPTZ)
```

#### 8. **جدول `admins` - المسؤولين**
```sql
- id (TEXT)
- email (TEXT) - بريد Admin (فريد)
- password (TEXT) - كلمة المرور (مشفرة)
- name (TEXT)
- last_login (TIMESTAMPTZ)
- created_at/updated_at
```

### **Storage Buckets (التخزين):**

| اسم Bucket | نوع الوصول | الاستخدام | الحد الأقصى |
|-----------|------------|-----------|-------------|
| `make-8a20c00b-cv-files` | خاص | السير الذاتية المرفوعة | 5MB |
| `make-8a20c00b-profile-images` | عام | صور البطاقات الرقمية | 2MB |
| `make-8a20c00b-digital-cvs` | خاص | سير ذاتية البطاقات | 5MB |
| `make-8a20c00b-news-images` | عام | صور الأخبار | 5MB |

---

## 🎯 صفحات التطبيق (28 صفحة)

### **صفحات عامة (Public Pages):**
1. `/` - الصفحة الرئيسية
2. `/about` - من نحن
3. `/contact` - تواصل معنا
4. `/news` - الأخبار
5. `/company-jobs` - وظائف الشركات الخاصة
6. `/government-jobs` - الوظائف الحكومية
7. `/job/:id` - تفاصيل الوظيفة
8. `/card/:id` - البطاقة الرقمية العامة
9. `/career-path` - المسار الوظيفي
10. `/scholarships` - المنح الدراسية
11. `/training` - التدريب
12. `/privacy-policy` - سياسة الخصوصية
13. `/terms-of-service` - شروط الخدمة

### **صفحات المستخدمين (User Pages - تتطلب تسجيل دخول):**
14. `/login` - تسجيل الدخول
15. `/register` - التسجيل
16. `/profile` - الملف الشخصي
17. `/premium` - صفحة الخدمات المميزة

### **صفحات Premium (تتطلب اشتراك Premium):**
18. `/premium/cover-letter` - توليد Cover Letter بالذكاء الاصطناعي
19. `/premium/digital-card` - إنشاء البطاقة الرقمية
20. `/premium/job-match` - حساب نسبة التوافق مع الوظائف
21. `/premium/ats-converter` - محول السيرة الذاتية لـ ATS

### **صفحات Admin (لوحة التحكم - تتطلب حساب Admin):**
22. `/admin/login` - تسجيل دخول الإدارة
23. `/admin/dashboard` - لوحة المعلومات الرئيسية
24. `/admin/jobs` - إدارة الوظائف (عرض، إضافة، تعديل، حذف)
25. `/admin/jobs/:id` - إضافة/تعديل وظيفة
26. `/admin/users` - إدارة المستخدمين (منح Premium، حذف)
27. `/admin/news` - إدارة الأخبار
28. `/admin/contact` - الرد على رسائل التواصل
29. `/admin/analytics` - الإحصائيات والتحليلات

---

## 🌟 الميزات الرئيسية

### **1. نظام الوظائف:**
- ✅ عرض الوظائف (حكومي + خاص)
- ✅ بحث وفلترة متقدمة
- ✅ تفاصيل الوظيفة الكاملة
- ✅ رابط التقديم المباشر
- ✅ تصنيف حسب النوع والموقع

### **2. خدمات Premium (4 خدمات):**

#### 📄 **Cover Letter Generator**
- توليد رسالة تعريف احترافية بالذكاء الاصطناعي
- رفع السيرة الذاتية (PDF/DOCX)
- إدخال بيانات الوظيفة
- استخدام GPT-4o-mini
- تحميل النتيجة كملف DOCX

#### 💳 **Digital Card**
- بطاقة رقمية شخصية قابلة للمشاركة
- رفع صورة شخصية + السيرة الذاتية
- إضافة روابط (LinkedIn, GitHub, Portfolio)
- رابط عام للمشاركة: `/card/:id`
- تحميل السيرة الذاتية من البطاقة

#### 📊 **Job Match Calculator**
- حساب نسبة التوافق بين السيرة الذاتية والوظيفة
- رفع CV (PDF/DOCX)
- تحليل بالذكاء الاصطناعي
- عرض النسبة المئوية + التفاصيل
- اقتراحات للتحسين

#### ✨ **ATS CV Converter**
- تحويل السيرة الذاتية لنسخة متوافقة مع ATS
- رفع CV بأي تنسيق
- تحسين الكلمات المفتاحية
- تنسيق صديق لأنظمة ATS
- تحميل النتيجة بتنسيق نظيف

### **3. لوحة تحكم Admin:**
- ✅ إحصائيات شاملة (Dashboard)
- ✅ إدارة الوظائف (CRUD كامل)
- ✅ إدارة المستخدمين (عرض، حذف، منح Premium)
- ✅ إدارة الأخبار (إضافة، تعديل، حذف)
- ✅ الرد على رسائل التواصل
- ✅ تحليلات تفصيلية (Charts)

### **4. نظام المستخدمين:**
- ✅ تسجيل حساب جديد (Email + Password)
- ✅ تسجيل الدخول
- ✅ الملف الشخصي القابل للتعديل
- ✅ حماية الصفحات (Protected Routes)
- ✅ JWT Authentication

### **5. نظام الأخبار:**
- ✅ عرض آخر الأخبار
- ✅ تصنيف الأخبار
- ✅ أخبار مميزة (Featured)
- ✅ رفع صور الأخبار

### **6. نظام التواصل:**
- ✅ صفحة "تواصل معنا"
- ✅ حفظ الرسائل في قاعدة البيانات
- ✅ Admin يقدر يرد على الرسائل
- ✅ تتبع حالة الرسالة (جديدة، مقروءة، تم الرد)

---

## 🔐 نظام الأمان

### **Row Level Security (RLS):**
- ✅ تفعيل RLS على جميع الجداول
- ✅ سياسات قراءة عامة للوظائف والأخبار
- ✅ الكتابة عبر `service_role` فقط من السيرفر
- ✅ حماية بيانات Admins (لا قراءة من Frontend)

### **Authentication:**
- ✅ JWT Tokens للمستخدمين
- ✅ تسجيل دخول Admin منفصل (email + password)
- ✅ حسابين Admin ثابتين فقط
- ✅ Protected Routes في Frontend

### **Environment Variables:**
```bash
SUPABASE_URL
SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY  # ⚠️ لا يتم مشاركتها
SUPABASE_DB_URL
OPENAI_API_KEY             # ⚠️ حساسة
```

---

## 🚀 كيفية عمل النظام

### **Architecture (البنية المعمارية):**

```
┌─────────────────────────────────────────────────────────┐
│                   USER (المستخدم)                       │
│                    (المتصفح)                            │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│              FRONTEND (React + TypeScript)               │
│  - صفحات (Pages)                                        │
│  - مكونات (Components)                                  │
│  - Routing (React Router)                               │
│  - Styling (Tailwind CSS)                               │
│  - API Calls (utils/api.ts)                             │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│      SUPABASE EDGE FUNCTIONS (Backend Server)           │
│  - Hono Web Framework                                   │
│  - Deno Runtime                                         │
│  - Endpoints:                                           │
│    • /make-server-8a20c00b/jobs (CRUD)                  │
│    • /make-server-8a20c00b/users (CRUD)                 │
│    • /make-server-8a20c00b/premium/* (AI Services)      │
│    • /make-server-8a20c00b/admin/* (Admin APIs)         │
│    • /make-server-8a20c00b/contact (رسائل التواصل)      │
└──────────────────┬──────────────────────────────────────┘
                   │
         ┌─────────┴──────────┐
         ▼                    ▼
┌──────────────────┐  ┌──────────────────┐
│   SUPABASE DB    │  │ SUPABASE STORAGE │
│   (PostgreSQL)   │  │  (File Storage)  │
│   - 8 جداول      │  │  - 4 Buckets     │
│   - RLS Policies │  │  - Images/CVs    │
└──────────────────┘  └──────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│        EXTERNAL SERVICES                │
│  - OpenAI API (GPT-4o-mini)             │
│    • Cover Letter Generation            │
│    • Job Match Calculation              │
│    • ATS CV Conversion                  │
└─────────────────────────────────────────┘
```

### **Data Flow (تدفق البيانات):**

#### مثال: إضافة وظيفة جديدة
```
1. Admin يملأ النموذج في /admin/jobs/new
2. Frontend يرسل POST request إلى:
   https://{projectId}.supabase.co/functions/v1/make-server-8a20c00b/admin/jobs
3. السيرفر يتحقق من البيانات
4. السيرفر يستخدم service_role للكتابة في جدول jobs
5. Supabase يحفظ البيانات
6. السيرفر يرجع الوظيفة الجديدة
7. Frontend يعرض رسالة نجاح ويعيد التوجيه
```

#### مثال: توليد Cover Letter
```
1. User يرفع CV + يدخل بيانات الوظيفة
2. Frontend يرسل POST إلى /premium/cover-letter
3. السيرفر:
   - يستخرج النص من PDF/DOCX
   - يرسل إلى OpenAI API
   - يستقبل Cover Letter المولد
4. السيرفر يعيد النتيجة
5. Frontend يعرض النتيجة + زر التحميل
```

---

## 📦 الحزم والمكتبات الرئيسية

### **Frontend Dependencies:**
```json
{
  "react": "^18.3.1",
  "react-dom": "^18.3.1",
  "react-router-dom": "^7.9.4",
  "typescript": "^5.6.2",
  "tailwindcss": "^4.0.0",
  "@supabase/supabase-js": "^2.x",
  "lucide-react": "latest",
  "recharts": "^2.x",
  "sonner": "^2.0.3",
  "date-fns": "^4.x",
  "clsx": "^2.x",
  "tailwind-merge": "^2.x"
}
```

### **Backend Dependencies (في Deno):**
```typescript
import { Hono } from "npm:hono";
import { cors } from "npm:hono/cors";
import { logger } from "npm:hono/logger";
import { createClient } from "npm:@supabase/supabase-js@2";
import OpenAI from "npm:openai@4";
// PDF/DOCX processing
```

---

## 🎨 التصميم والواجهة

### **خصائص التصميم:**
- ✅ **RTL Support**: دعم كامل للعربية
- ✅ **Responsive**: متجاوب مع جميع الأجهزة (Mobile, Tablet, Desktop)
- ✅ **Dark Mode Ready**: جاهز لوضع الليل (غير مفعّل حالياً)
- ✅ **Accessibility**: دعم ARIA labels
- ✅ **Fast Loading**: تحميل سريع مع Vite

### **نظام الألوان:**
```css
/* Primary Colors */
--red-600: #dc2626;        /* اللون الأحمر الرئيسي */
--red-700: #b91c1c;        /* أحمر داكن */

/* Neutral Colors */
--gray-50: #f9fafb;        /* خلفية فاتحة */
--gray-100: #f3f4f6;       /* خلفية بطاقات */
--gray-600: #4b5563;       /* نصوص ثانوية */
--gray-900: #111827;       /* نصوص رئيسية */
```

### **Typography:**
- **Font Family**: system fonts (عربي + إنجليزي)
- **Font Sizes**: من text-xs إلى text-6xl
- **Font Weights**: من font-normal إلى font-bold

---

## 🔄 APIs والـ Endpoints

### **Public APIs:**
```
GET  /make-server-8a20c00b/jobs              # جلب جميع الوظائف
GET  /make-server-8a20c00b/jobs/:id          # جلب وظيفة واحدة
GET  /make-server-8a20c00b/news              # جلب الأخبار
POST /make-server-8a20c00b/contact           # إرسال رسالة تواصل
```

### **User APIs:**
```
POST /make-server-8a20c00b/register          # تسجيل مستخدم جديد
POST /make-server-8a20c00b/login             # تسجيل الدخول
GET  /make-server-8a20c00b/profile/:id       # جلب الملف الشخصي
PUT  /make-server-8a20c00b/profile/:id       # تحديث الملف الشخصي
```

### **Premium APIs:**
```
POST /make-server-8a20c00b/premium/cover-letter    # توليد Cover Letter
POST /make-server-8a20c00b/premium/job-match       # حساب التوافق
POST /make-server-8a20c00b/premium/ats-convert     # تحويل ATS
POST /make-server-8a20c00b/digital-card            # إنشاء بطاقة رقمية
GET  /make-server-8a20c00b/digital-card/:id        # جلب بطاقة رقمية
```

### **Admin APIs:**
```
POST   /make-server-8a20c00b/admin/login           # تسجيل دخول Admin
POST   /make-server-8a20c00b/admin/jobs            # إضافة وظيفة
PUT    /make-server-8a20c00b/admin/jobs/:id        # تحديث وظيفة
DELETE /make-server-8a20c00b/admin/jobs/:id        # حذف وظيفة
GET    /make-server-8a20c00b/admin/users           # جلب المستخدمين
POST   /make-server-8a20c00b/admin/users/:id/premium  # منح Premium
DELETE /make-server-8a20c00b/admin/users/:id       # حذف مستخدم
GET    /make-server-8a20c00b/admin/contact         # جلب رسائل التواصل
PUT    /make-server-8a20c00b/admin/contact/:id     # الرد على رسالة
POST   /make-server-8a20c00b/admin/news            # إضافة خبر
PUT    /make-server-8a20c00b/admin/news/:id        # تحديث خبر
DELETE /make-server-8a20c00b/admin/news/:id        # حذف خبر
GET    /make-server-8a20c00b/admin/analytics       # جلب الإحصائيات
```

---

## ✅ حالة المشروع

### **ما يعمل بشكل كامل:**
- ✅ نظام الوظائف (CRUD كامل)
- ✅ نظام المستخدمين (تسجيل، دخول، ملف شخصي)
- ✅ 4 خدمات Premium نشطة
- ✅ لوحة تحكم Admin شاملة
- ✅ نظام الأخبار
- ✅ نظام التواصل
- ✅ البطاقات الرقمية
- ✅ RLS Security
- ✅ JWT Authentication
- ✅ File Upload (Storage)
- ✅ OpenAI Integration

### **ما لم يُطور بعد:**
- ❌ نظام الدفع الإلكتروني (حالياً: Admin يمنح Premium يدوياً)
- ❌ إشعارات Email
- ❌ Chat/Messaging بين المستخدمين
- ❌ Social Login (Google, Facebook)

---

## 📊 إحصائيات المشروع

| البند | العدد |
|------|-------|
| **عدد الصفحات** | 28 صفحة |
| **عدد المكونات** | 50+ مكون |
| **عدد الـ API Endpoints** | 30+ endpoint |
| **عدد الجداول** | 8 جداول |
| **عدد Storage Buckets** | 4 buckets |
| **أسطر الكود (تقريبي)** | 15,000+ سطر |
| **ملفات التوثيق** | 35+ ملف |

---

## 🛠️ كيفية الاستخدام

### **للمطورين:**
1. قراءة [`SETUP.md`](./SETUP.md) لإعداد البيئة المحلية
2. استنساخ المشروع وتثبيت الحزم
3. إعداد Supabase وقاعدة البيانات
4. تشغيل `npm run dev`

### **للمستخدمين العاديين:**
1. التسجيل في الموقع
2. تصفح الوظائف المتاحة
3. الاشتراك في Premium للوصول للأدوات الذكية

### **للـ Admins:**
1. تسجيل الدخول عبر `/admin/login`
2. إدارة الوظائف والمستخدمين
3. الرد على رسائل التواصل
4. متابعة الإحصائيات

---

## 🔮 المستقبل

### **ميزات مخطط إضافتها:**
- 🔄 نظام دفع إلكتروني (Stripe/PayPal)
- 📧 إشعارات Email تلقائية
- 🔔 نظام إشعارات داخلي
- 📱 تطبيق موبايل (React Native)
- 🌐 لغات إضافية (English)
- 💬 Chat بين المستخدمين والشركات
- 📈 تحليلات متقدمة أكثر
- 🤖 AI Chatbot للمساعدة

---

## 📞 الدعم

للأسئلة والمساعدة:
- 📖 راجع ملفات التوثيق في المجلد الرئيسي
- 📧 تواصل مع مالك المشروع
- 🐛 افتح Issue على GitHub

---

## 📝 ملاحظات مهمة

### **⚠️ قبل المشاركة:**
- احذف جميع البيانات الحساسة
- لا تشارك `.env.local`
- راجع [`SHARING_GUIDE.md`](./SHARING_GUIDE.md)

### **🔒 أمان:**
- جميع الـ API Keys في Environment Variables
- RLS مفعّل على جميع الجداول
- حماية صفحات Admin
- JWT للمصادقة

---

<div align="center">

## 🎉 المشروع جاهز للاستخدام

**منصة عُمان للوظائف** - موقع متكامل لنشر الوظائف مع أدوات ذكاء اصطناعي احترافية

**صُنع بـ ❤️ في عُمان**

---

_آخر تحديث: ديسمبر 2024_

</div>

</div>
