# ❓ الأسئلة الشائعة - نظام جلب الوظائف
# FAQ - Job Scraper System

## 📌 أسئلة عامة

### س: ما هو نظام جلب الوظائف التلقائي؟
**ج:** هو نظام يقوم بزيارة مواقع الوظائف الخارجية (مثل jobsofoman.com) تلقائياً، واستخراج الوظائف الجديدة، وإضافتها إلى منصة عُمان للوظائف دون أي تدخل يدوي.

### س: هل النظام مجاني؟
**ج:** نعم، النظام مجاني تماماً. يمكن جدولته باستخدام خدمات مجانية مثل cron-job.org أو GitHub Actions.

### س: هل يحتاج النظام إلى صيانة دورية؟
**ج:** نادراً. قد تحتاج لتحديث selectors إذا تغيرت بنية HTML للموقع المصدر، ولكن هذا يحدث نادراً.

### س: كم عدد الوظائف التي يمكن جلبها في كل مرة؟
**ج:** يعتمد على الموقع المصدر. عادة بين 20-100 وظيفة في كل عملية جلب.

---

## 🔧 أسئلة تقنية

### س: ما هي التقنيات المستخدمة؟
**ج:**
- **Backend**: Deno + Hono framework
- **Web Scraping**: deno_dom (DOM Parser)
- **Database**: Supabase PostgreSQL
- **Frontend**: React + TypeScript
- **Styling**: Tailwind CSS

### س: هل يمكن إضافة مواقع أخرى غير jobsofoman.com؟
**ج:** نعم! يمكنك بسهولة إضافة مواقع جديدة عن طريق:
1. إنشاء دالة scraper جديدة في `job-scraper.tsx`
2. تحديث دالة `scrapeWebsite` لدعم الموقع الجديد

```typescript
export async function scrapeNewSite(): Promise<ScrapedJob[]> {
  // كود الـ scraper الجديد
}
```

### س: كيف يتجنب النظام الوظائف المكررة؟
**ج:** النظام يستخدم طبقتين:
1. **في الجلسة**: تجنب إضافة نفس الوظيفة مرتين في نفس عملية الجلب
2. **في قاعدة البيانات**: مقارنة العنوان ورابط التقديم مع الوظائف الموجودة

### س: ما هي مدة timeout لعملية الجلب؟
**ج:** 30 ثانية. إذا لم يستجب الموقع خلال 30 ثانية، تفشل العملية تلقائياً.

---

## 🚀 أسئلة الاستخدام

### س: كيف أعرف أن النظام يعمل بشكل صحيح؟
**ج:** تحقق من:
- ✅ رسالة نجاح في صفحة Admin
- ✅ عدد الوظائف المضافة > 0
- ✅ الوظائف تظهر في قائمة الوظائف
- ✅ لا توجد أخطاء في console

### س: ما هو أفضل توقيت لجدولة عملية الجلب؟
**ج:** يُنصح بـ:
- **مرة يومياً**: الساعة 2-3 صباحاً (وقت هادئ)
- **مرتين يومياً**: 2 صباحاً و 2 ظهراً
- **كل 6 ساعات**: للمواقع النشطة جداً

**تجنب:**
- ❌ كل ساعة (تحميل زائد)
- ❌ أكثر من 4 مرات يومياً

### س: هل يمكن تشغيل العملية يدوياً؟
**ج:** نعم! من صفحة `/admin/scraper`، اضغط على زر "جلب الوظائف الآن".

### س: ماذا أفعل إذا لم تضف أي وظائف جديدة؟
**ج:** هذا طبيعي في الحالات التالية:
- جميع الوظائف موجودة مسبقاً (مكررة)
- الموقع المصدر لم ينشر وظائف جديدة
- تم تشغيل العملية مؤخراً

لا حاجة لفعل شيء.

---

## 🐛 أسئلة حل المشاكل

### س: "لم يتم العثور على وظائف" - ما الحل؟
**ج:** الأسباب المحتملة:
1. **تغيير في بنية الموقع**: افحص الموقع يدوياً وحدّث selectors
2. **الموقع لا يعمل**: تحقق من أن الموقع المصدر يعمل
3. **مشكلة في الشبكة**: أعد المحاولة لاحقاً

**الحل:**
```typescript
// في job-scraper.tsx، حدّث selectors
const possibleSelectors = [
  '.new-class-name',  // أضف selector جديد
  // ... بقية selectors
];
```

### س: حصلت على خطأ "timeout" - ما الحل؟
**ج:** هذا يحدث إذا كان الموقع بطيئاً. الحلول:
1. أعد المحاولة بعد دقائق
2. زد timeout في الكود:
```typescript
const timeoutId = setTimeout(() => controller.abort(), 60000); // 60 ثانية
```
3. تحقق من اتصال الإنترنت

### س: الوظائف المستخرجة غير صحيحة أو مشوهة - ما الحل؟
**ج:** حسّن الـ filtering والتنظيف:
```typescript
// تحقق من جودة البيانات
if (!title || title.length < 10 || title.length > 200) continue;
if (!description || description.length < 20) continue;

// نظف البيانات
title = title.trim().replace(/\s+/g, ' ');
description = description.trim().substring(0, 500);
```

### س: "خطأ في قاعدة البيانات" - ما الحل؟
**ج:** تحقق من:
1. اتصال Supabase
2. RLS policies (يجب السماح بالـ insert)
3. صلاحيات Service Role Key
4. بنية جدول jobs

**للفحص:**
```sql
-- في Supabase SQL Editor
SELECT * FROM jobs LIMIT 1;
```

---

## 📊 أسئلة الأداء

### س: كم تستغرق عملية الجلب؟
**ج:** عادة:
- ⚡ سريع: 5-10 ثوانٍ
- 🟢 عادي: 10-20 ثانية
- 🟡 بطيء: 20-30 ثانية
- 🔴 مشكلة: أكثر من 30 ثانية

### س: هل يؤثر النظام على أداء الموقع؟
**ج:** لا. عملية الجلب تحدث في background على السيرفر ولا تؤثر على تجربة المستخدمين.

### س: كم يستهلك من الموارد؟
**ج:**
- **Bandwidth**: ~100-500 KB لكل عملية
- **Memory**: ~10-20 MB
- **CPU**: قليل جداً
- **Database**: 1-2 queries لكل عملية

---

## 🔐 أسئلة الأمان

### س: هل النظام آمن؟
**ج:** نعم، بشرط:
- ✅ حماية admin token
- ✅ عدم مشاركة credentials
- ✅ استخدام HTTPS فقط
- ✅ تحديث dependencies بانتظام

### س: هل يمكن لأي شخص تشغيل عملية الجلب؟
**ج:** لا. فقط:
- Admin مع token صحيح
- Cron jobs المعدّة بشكل صحيح
- Requests مع X-Admin-Token header

### س: هل يخالف scraping شروط استخدام المواقع؟
**ج:** يعتمد على الموقع. عموماً:
- ✅ استخدم بمعقولية (لا تبالغ)
- ✅ احترم robots.txt
- ✅ استخدم User-Agent واضح
- ✅ لا تحمّل الموقع بطلبات كثيرة

---

## 💡 أسئلة متقدمة

### س: كيف أضيف authentication للـ endpoint؟
**ج:** حدّث middleware في `index.tsx`:
```typescript
const isAdmin = async (c: any, next: any) => {
  const token = c.req.header("X-Admin-Token");
  
  if (!token || !verifyToken(token)) {
    return c.json({ error: "Unauthorized" }, 401);
  }
  
  await next();
};

app.post("/make-server-8a20c00b/admin/scrape-jobs", isAdmin, async (c) => {
  // ...
});
```

### س: كيف أضيف logging متقدم؟
**ج:**
```typescript
import * as log from "https://deno.land/std/log/mod.ts";

await log.setup({
  handlers: {
    file: new log.handlers.FileHandler("INFO", {
      filename: "./logs/scraper.log",
    }),
  },
  loggers: {
    default: {
      level: "INFO",
      handlers: ["file"],
    },
  },
});

log.info("Starting job scraping...");
```

### س: كيف أضيف إشعارات عند النجاح/الفشل؟
**ج:** في `index.tsx` بعد عملية الجلب:
```typescript
// إرسال إشعار webhook
if (insertedJobs && insertedJobs.length > 0) {
  await fetch('https://webhook.site/YOUR_WEBHOOK', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      message: `Added ${insertedJobs.length} new jobs`,
      jobs: insertedJobs
    })
  });
}
```

### س: كيف أجعل الـ scraper أكثر ذكاءً باستخدام AI؟
**ج:** يمكنك استخدام OpenAI لتحسين البيانات:
```typescript
// بعد الجلب، حسّن البيانات
const improvedDescription = await fetch('https://api.openai.com/v1/chat/completions', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${Deno.env.get('OPENAI_API_KEY')}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    model: 'gpt-4o-mini',
    messages: [{
      role: 'user',
      content: `حسّن وصف الوظيفة التالي: ${description}`
    }]
  })
});
```

---

## 🌐 أسئلة التوسع

### س: هل يمكن استخدام النظام لمواقع بلغات أخرى؟
**ج:** نعم! النظام يدعم:
- العربية (الحالي)
- الإنجليزية
- أي لغة أخرى (بتعديل selectors)

### س: كم عدد المواقع التي يمكن جلب الوظائف منها؟
**ج:** لا يوجد حد. يمكنك إضافة عدد غير محدود من المواقع، فقط:
- أضف scraper function لكل موقع
- حدّث `scrapeWebsite` function
- جدول عمليات منفصلة لكل موقع

### س: هل يمكن جلب وظائف من مواقع تتطلب تسجيل دخول؟
**ج:** نعم، لكن أصعب. ستحتاج إلى:
1. إدارة cookies/sessions
2. التعامل مع CSRF tokens
3. استخدام headless browser (Puppeteer/Playwright)

مثال:
```typescript
import { chromium } from "npm:playwright@1.40.0";

const browser = await chromium.launch();
const page = await browser.newPage();
await page.goto('https://site-with-login.com/login');
await page.fill('#email', 'user@example.com');
await page.fill('#password', 'password');
await page.click('button[type="submit"]');
// ... جلب الوظائف بعد تسجيل الدخول
```

---

## 📈 أسئلة الإحصائيات

### س: كيف أتتبع عدد الوظائف المجلوبة يومياً؟
**ج:** أضف logging في السيرفر:
```typescript
// في index.tsx بعد الإضافة
await supabase.from('scraping_logs').insert([{
  date: new Date().toISOString(),
  jobs_scraped: scrapedJobs.length,
  jobs_added: insertedJobs.length,
  source: 'jobsofoman.com'
}]);
```

ثم أنشئ جدول `scraping_logs` في Supabase:
```sql
CREATE TABLE scraping_logs (
  id SERIAL PRIMARY KEY,
  date TIMESTAMP NOT NULL,
  jobs_scraped INTEGER NOT NULL,
  jobs_added INTEGER NOT NULL,
  source TEXT NOT NULL,
  created_at TIMESTAMP DEFAULT NOW()
);
```

### س: كيف أعرف أي المصادر أكثر إنتاجية؟
**ج:** استخدم query:
```sql
SELECT 
  source,
  COUNT(*) as total_runs,
  AVG(jobs_added) as avg_jobs_per_run,
  SUM(jobs_added) as total_jobs_added
FROM scraping_logs
WHERE date >= NOW() - INTERVAL '30 days'
GROUP BY source
ORDER BY total_jobs_added DESC;
```

---

## 🎯 أسئلة الأهداف

### س: ما هو العدد المثالي للوظائف يومياً؟
**ج:** يعتمد على:
- حجم موقعك
- عدد المستخدمين
- نشاط سوق العمل

**مثالي:**
- 🟢 10-30 وظيفة يومياً (جيد للبداية)
- 🟡 30-100 وظيفة يومياً (نمو جيد)
- 🔵 100+ وظيفة يومياً (موقع كبير)

### س: كيف أحسّن جودة الوظائف المجلوبة؟
**ج:**
1. **Filtering أفضل**: تجنب وظائف غير مناسبة
2. **Data cleaning**: تنظيف العناوين والأوصاف
3. **Deduplication محسّن**: تجنب التكرار بشكل أفضل
4. **Manual review**: مراجعة دورية يدوية

---

## 🔄 أسئلة الصيانة

### س: كم مرة يجب أن أراجع النظام؟
**ج:**
- **يومياً**: في البداية (أول أسبوع)
- **أسبوعياً**: بعد الاستقرار
- **شهرياً**: للمواقع المستقرة

### س: ماذا أفعل عند تحديث الموقع المصدر؟
**ج:**
1. راقب أي انخفاض مفاجئ في عدد الوظائف
2. افحص الموقع يدوياً
3. حدّث selectors إذا لزم الأمر
4. اختبر التحديثات

### س: كيف أحذف الوظائف القديمة؟
**ج:** يمكنك:

**يدوياً:**
```sql
DELETE FROM jobs 
WHERE date < NOW() - INTERVAL '90 days';
```

**تلقائياً (Supabase):**
أنشئ function مجدولة تحذف الوظائف القديمة

---

## 💬 أسئلة الدعم

### س: أين أجد المساعدة؟
**ج:**
1. راجع ملفات التوثيق:
   - `SCRAPER_SYSTEM_README.md`
   - `JOB_SCRAPER_GUIDE.md`
   - `QUICK_START_SCRAPER.md`
2. افحص console logs
3. راجع Supabase logs
4. اتصل بمطور النظام

### س: كيف أبلّغ عن مشكلة؟
**ج:** اجمع المعلومات التالية:
- رسالة الخطأ كاملة
- Console logs
- Supabase logs
- خطوات إعادة إنتاج المشكلة
- الإصدار والبيئة

---

## 🎓 أسئلة التعلم

### س: أين أتعلم المزيد عن Web Scraping؟
**ج:**
- [Deno Documentation](https://deno.land/manual)
- [deno_dom GitHub](https://github.com/b-fuze/deno-dom)
- [Web Scraping Best Practices](https://www.scrapingbee.com/blog/web-scraping-best-practices/)

### س: كيف أتعلم Supabase Edge Functions؟
**ج:**
- [Supabase Edge Functions Docs](https://supabase.com/docs/guides/functions)
- [Deno Deploy Guide](https://deno.com/deploy/docs)

---

## ✅ الخلاصة

**الأسئلة الأكثر شيوعاً:**

1. ✅ **كيف أبدأ؟** → راجع `QUICK_START_SCRAPER.md`
2. ✅ **لا تضاف وظائف** → طبيعي إذا كانت مكررة
3. ✅ **كيف أجدول تلقائياً؟** → استخدم cron-job.org أو GitHub Actions
4. ✅ **هل آمن؟** → نعم، بشرط حماية tokens
5. ✅ **هل مجاني؟** → نعم تماماً

---

**ملاحظة:** إذا كان سؤالك غير موجود هنا، راجع الملفات التوثيقية الأخرى أو اتصل بالدعم.

🎉 **نتمنى لك تجربة ممتعة مع نظام جلب الوظائف!**
