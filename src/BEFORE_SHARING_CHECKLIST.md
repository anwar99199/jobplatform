# ✅ قائمة التحقق قبل مشاركة المشروع

**⚠️ اقرأ هذا الملف كاملاً قبل مشاركة المشروع مع أي شخص!**

---

## 🚨 خطوات إلزامية قبل المشاركة

### 1️⃣ تنظيف بيانات Admin من الكود

#### ✅ **يجب عليك:**

**افتح `/supabase/functions/server/index.tsx`**

**ابحث عن السطر ~299-302:**

```typescript
// ❌ احذف أو عدّل هذا الكود:
const ALLOWED_ADMINS = [
  { email: "as8543245@gmail.com", password: "A1999anw#" },
  { email: "anwaralrawahi459@gmail.com", password: "6101999" }
];
```

**استبدله بـ:**

```typescript
// ✅ الخيار 1: استخدام Environment Variables (موصى به)
const ALLOWED_ADMINS = [
  { 
    email: Deno.env.get('ADMIN_EMAIL_1') || '', 
    password: Deno.env.get('ADMIN_PASSWORD_1') || '' 
  },
  { 
    email: Deno.env.get('ADMIN_EMAIL_2') || '', 
    password: Deno.env.get('ADMIN_PASSWORD_2') || '' 
  }
];

// ✅ الخيار 2: اجعلها تعليقات
const ALLOWED_ADMINS = [
  { email: "YOUR_ADMIN_EMAIL_1", password: "YOUR_PASSWORD_1" },
  { email: "YOUR_ADMIN_EMAIL_2", password: "YOUR_PASSWORD_2" }
];
```

**ابحث أيضاً عن السطر ~2467:**

```typescript
// ❌ احذف أو عدّل:
const adminEmails = ['as8543245@gmail.com', 'anwaralrawahi459@gmail.com'];

// ✅ استبدل بـ:
const adminEmails = [
  Deno.env.get('ADMIN_EMAIL_1') || '',
  Deno.env.get('ADMIN_EMAIL_2') || ''
];
```

---

### 2️⃣ حذف/حماية ملفات Environment Variables

```bash
# ✅ تأكد من حذف:
rm .env
rm .env.local
rm .env.production

# ✅ تأكد من وجود:
.env.example ← موجود ✓
.gitignore ← موجود ✓
```

---

### 3️⃣ مراجعة الملفات الحساسة

```bash
# ✅ ابحث عن أي بيانات حساسة:
grep -r "as8543245" .
grep -r "anwaralrawahi459" .
grep -r "A1999anw" .
grep -r "6101999" .

# يجب أن تظهر فقط في:
# - /supabase/functions/server/index.tsx (إذا لم تحذفها بعد)
# - هذا الملف (BEFORE_SHARING_CHECKLIST.md)

# ❌ يجب ألا تظهر في:
# - أي ملف .md آخر
# - أي ملف .tsx آخر
# - أي ملف .ts آخر
# - أي ملف .sql
```

---

### 4️⃣ التحقق من .gitignore

**تأكد من أن `.gitignore` يحتوي على:**

```gitignore
# Environment Variables
.env
.env.local
.env.production

# Secrets
*.secret
secrets.txt

# Personal files
NOTES.md
TODO.md
PRIVATE.md
```

---

### 5️⃣ إنشاء الملفات المطلوبة

```bash
✅ .gitignore           ← موجود
✅ .env.example         ← موجود
✅ README.md            ← موجود
✅ SETUP.md             ← موجود
✅ SHARING_GUIDE.md     ← موجود
```

---

## 📋 قائمة التحقق الشاملة

### قبل المشاركة مباشرة:

```
☐ حذفت بيانات Admin من /supabase/functions/server/index.tsx
☐ حذفت جميع ملفات .env
☐ تأكدت من وجود .env.example
☐ تأكدت من وجود .gitignore
☐ بحثت عن بيانات حساسة في الكود (grep)
☐ راجعت جميع ملفات .md
☐ حذفت أي ملفات شخصية (notes, todos)
☐ اختبرت النسخة النظيفة
☐ أنشأت README.md شامل
☐ أنشأت SETUP.md مفصل
```

---

## 🧪 اختبار النسخة النظيفة

### **قبل المشاركة، اختبر:**

```bash
# 1. انسخ المشروع في مجلد جديد
cp -r oman-jobs-platform oman-jobs-platform-clean

# 2. احذف بيانات Environment
cd oman-jobs-platform-clean
rm .env .env.local 2>/dev/null

# 3. انسخ .env.example
cp .env.example .env.local

# 4. حاول تشغيل المشروع
npm install
npm run dev

# 5. يجب أن يظهر خطأ في Environment Variables
# (هذا طبيعي - المطور الجديد سيملأها بنفسه)
```

---

## 🔍 البحث عن البيانات الحساسة

### **نفّذ هذه الأوامر:**

```bash
# البحث في جميع الملفات:
grep -r "as8543245" . --exclude-dir=node_modules
grep -r "anwaralrawahi459" . --exclude-dir=node_modules
grep -r "A1999anw" . --exclude-dir=node_modules
grep -r "6101999" . --exclude-dir=node_modules

# البحث عن API Keys:
grep -r "sk-" . --exclude-dir=node_modules
grep -r "SUPABASE_SERVICE_ROLE_KEY" . --exclude-dir=node_modules

# البحث عن Passwords:
grep -r "password.*=.*['\"]" . --exclude-dir=node_modules --include="*.tsx" --include="*.ts"
```

**⚠️ إذا وجدت أي نتائج (غير المتوقعة)، احذفها!**

---

## 📝 ما يجب توفيره للمطور الجديد

### ✅ شارك:

```
✅ الكود النظيف (بدون بيانات حساسة)
✅ README.md (دليل البداية)
✅ SETUP.md (دليل الإعداد المفصل)
✅ .env.example (مثال Environment Variables)
✅ .gitignore
✅ ملفات التوثيق (.md)
✅ SQL migration files
```

### ❌ لا تشارك:

```
❌ ملفات .env
❌ بيانات Admin الفعلية
❌ API Keys الفعلية
❌ Supabase Credentials الفعلية
❌ أي بيانات إنتاج
❌ ملفات شخصية (notes, todos)
```

---

## 💬 رسالة للمطور الجديد

**عند المشاركة، أرسل:**

```
مرحباً،

لقد شاركت معك مشروع منصة عُمان للوظائف.

📚 ابدأ من هنا:
1. اقرأ README.md أولاً
2. اتبع SETUP.md خطوة بخطوة
3. ستحتاج إلى:
   - حساب Supabase (مجاني)
   - مفتاح OpenAI API (مدفوع)
   - تعديل بيانات Admin في الكود

⚠️ ملاحظات أمنية:
- لا تشارك أي بيانات من .env
- لا ترفع الكود على GitHub public (إلا إذا نظفته)
- غيّر جميع الـ secrets

📞 تواصل معي إذا احتجت مساعدة!

التوفيق 🚀
```

---

## 🎯 طرق المشاركة الآمنة

### **الخيار 1: GitHub Private Repository (موصى به)**

```bash
# 1. أنشئ repo خاص على GitHub
# 2. تأكد من تنظيف الكود
# 3. رفع الكود:

git init
git add .
git commit -m "Initial commit"
git remote add origin your-private-repo-url
git push -u origin main

# 4. أضف المطور الآخر كـ Collaborator
```

---

### **الخيار 2: ZIP File**

```bash
# 1. تأكد من تنظيف الكود
# 2. احذف node_modules:
rm -rf node_modules

# 3. إنشاء ZIP:
zip -r oman-jobs-platform.zip . -x "node_modules/*" -x ".git/*" -x ".env*"

# 4. شارك الملف المضغوط
```

---

### **الخيار 3: Google Drive / Dropbox**

```
1. اتبع نفس خطوات ZIP
2. ارفع على Drive أو Dropbox
3. اجعله متاحاً فقط للمطور المحدد
4. لا تجعله public!
```

---

## ⚠️ بعد المشاركة

### **للأمان الإضافي:**

```
1. ✅ غيّر بيانات Admin الخاصة بك
2. ✅ راقب نشاط Supabase Dashboard
3. ✅ تحقق من Logs بانتظام
4. ✅ لا تعطي وصول كامل لقاعدة البيانات الإنتاج
5. ✅ أنشئ environment منفصل للتطوير
```

---

## 🔄 مراجعة Git History

```bash
# تحقق من عدم وجود commits قديمة تحتوي على بيانات حساسة:

git log --all --full-history -- .env
git log --all --full-history -- *.secret

# إذا وجدت commits تحتوي على بيانات حساسة:
# يجب تنظيف Git history (متقدم!)
# أو إنشاء repo جديد نظيف
```

---

## 🎓 نصائح للمطور الجديد

**ضعها في README أو أرسلها مع المشروع:**

```markdown
### للمطور الجديد:

1. 📖 اقرأ جميع ملفات .md قبل البدء
2. 🔐 لا تشارك أي بيانات حساسة مع أي شخص
3. ✅ استخدم Environment Variables دائماً
4. 🧪 اختبر كل ميزة بعد الإعداد
5. 💾 احتفظ بنسخة احتياطية من قاعدة البيانات
6. 📞 تواصل معي عند أي مشكلة
```

---

## ✅ الخلاصة النهائية

```
╔═════════════════════════════════════════════════╗
║                                                 ║
║  🚨 قبل المشاركة (إلزامي):                   ║
║                                                 ║
║  1. احذف بيانات Admin من الكود                ║
║  2. احذف ملفات .env                           ║
║  3. راجع جميع الملفات                          ║
║  4. ابحث عن بيانات حساسة (grep)               ║
║  5. اختبر النسخة النظيفة                       ║
║                                                 ║
║  ✅ بعد المشاركة (موصى به):                  ║
║                                                 ║
║  1. غيّر بيانات Admin                         ║
║  2. راقب نشاط قاعدة البيانات                  ║
║  3. وفّر الدعم للمطور الجديد                  ║
║                                                 ║
║  📚 ملفات يجب أن تكون موجودة:                ║
║                                                 ║
║  ✓ README.md                                   ║
║  ✓ SETUP.md                                    ║
║  ✓ .env.example                                ║
║  ✓ .gitignore                                  ║
║  ✓ SHARING_GUIDE.md                            ║
║                                                 ║
╚═════════════════════════════════════════════════╝
```

---

## 🎉 جاهز للمشاركة!

**إذا أكملت جميع النقاط أعلاه، المشروع جاهز للمشاركة بأمان!** ✅

---

**⚠️ تذكّر:** الأمان أهم من السرعة. خذ وقتك وراجع كل شيء!

---

_آخر تحديث: 3 ديسمبر 2025_
